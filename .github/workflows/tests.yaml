name: Run tests

on:
  pull_request:
  push:
  release:
    types:
      - published

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]

    name: Test Suite
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Install X server and libGL dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y xvfb libgl1-mesa-dev libglu1-mesa-dev libxi-dev libxrender-dev
      - name: Start X server in the background
        run: |
          Xvfb :99 -screen 0 1024x768x24 > /dev/null 2>&1 &
          echo "DISPLAY=:99" >> $GITHUB_ENV
      - uses: ./.github/actions/conda-setup
        with:
          python-version: ${{ matrix.python-version }}
      - name: Test environment conda package list
        shell: bash -l {0}
        run: |
          conda list
      - name: OpenUSD check
        shell: bash -l {0}
        run: |
          set +xe
          which pytest
          which python
          python -c 'import numpy; from pxr import Gf, Sdf, Usd, UsdGeom, UsdShade, Vt'
          pytest -v test_pxr.py
          python -m pytest -v test_pxr.py
          exit 0
      - name: Run unit tests
        shell: bash -l -e -o pipefail {0}
        run: |
          echo -e '## Test results\n\n```' >> "$GITHUB_STEP_SUMMARY"
          python -m pytest -v 2>&1 | tee -a "$GITHUB_STEP_SUMMARY"
          echo '```' >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        with:
          name: test-artifacts
          path: bpy_lattice/tests/artifacts/
